/** The three arrays every view reads from (see docs/architecture/ARCHITECTURE.md). Nothing else is a source of truth. */
export { BUILDINGS } from "./buildings.ts";
export { LOTS } from "./lots.ts";
export { GARAGES } from "./garages.ts";
